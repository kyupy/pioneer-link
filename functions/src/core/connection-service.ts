import type { OrgConnection } from "../shared/types";

// Placeholder for cross-org collaboration (later phase)

export function getConnections(_orgId: string): Promise<OrgConnection[]> {
  throw new Error("Not implemented: cross-org connections are planned for a later phase");
}
